from setuptools import setup, find_packages

setup(
    name='aesthetic-predictor',
    version='1.0',
    description='',
    packages=find_packages(),
    install_requires=[
        'torch',
        'numpy'
    ],
)